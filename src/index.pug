doctype html
html
  head
    title Webpack with Pug, Less and TypeScript!
  body
    header
      button#Home( onclick="window.router.load('home')" ) Home 
      button#About( onclick="window.router.load('about')" ) About
      button#Contact( onclick="window.router.load('contact')" ) Contact
      //button( (click)="func()", [if]="condition" ) {variable}
    router
